{
  "name": "flightiq",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "test": "npm --prefix backend test && npm --prefix frontend run lint",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
